
name: 02 workflow events

on:
  push:
  workflow_dispatch:

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
          - name: show the trigger
            run: echo "thiis is triggered by a(n) ${{ github.event_name }} event"
